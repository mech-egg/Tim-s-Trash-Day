--tim's terrible trash day
--framework

--can do
--crits
--power logic for plr
--shutdown and player actions
--pu obj system with 
--pu rnd logic + mods
--max inv shutoff
--progression
--gpile objs +placement

function _init()
	state='start'
	t=0
	wt=0
	debug=0
end

function _update()
	t+=1
	
	if state=='start' then
		update_start()
	elseif state=='game' then
		update_game()
	elseif state=='over' then
		update_over()
	end
end

function _draw()
	cls()
	if state=='start' then
		draw_start()
	elseif state=='game' then
		draw_game()
	elseif state=='over' then
		draw_over()
	end
	print(debug,6,120,7)
end