--update

function update_start()
	if btnp(🅾️) then
		gamestart()
	end
end

function gamestart()
	--music
	--state
	state='game'
	units={}
	enms={}
	flts={}
	pickups={}
	garbage={}
	scrapcount=0
	batcount=0
	create_plr()
	--spawn_gpile()
end

function update_game()
	move_plr()
	plr_logic()
	if btnp(🅾️) then
		state='over'
	end
end

function update_over()
	if btnp(🅾️) then 
		state='start'
	end
end

