--update

function update_start()
	if btnp(🅾️) then
		gamestart()
	end
end

function gamestart()
	--music
	--state
	state='game'
	units={}
	enms={}
	flts={}
	pickups={}
	garbage={}
	enemyfreq=120
	scrapcount=10
	batcount=10
	create_plr()
	--spawn_gpile()
end

function update_game()
	plr_logic()
	tman_logic()
	update_gpile()
	if btnp(🅾️) then
		state='over'
	end
end

function update_over()
	if btnp(🅾️) then 
		state='start'
	end
end

